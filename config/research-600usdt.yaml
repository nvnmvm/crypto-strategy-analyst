# Frozen v0.1.3 research profile. Legacy *_cny field names carry USDT when the
# account currency is USDT and the quote-to-account rate is exactly 1.0.
config_version: 1
risk:
  account_currency: USDT
  account_equity_cny: 600.0
  cny_per_usdt: 1.0
  risk_per_trade: 0.01
  aggregate_open_risk_limit: 0.02
  max_symbol_deployed_fraction: 0.60
  max_total_deployed_fraction: 1.0
  reserved_cash_fraction: 0.0
strategy:
  strategy_variant: relaxed_trend_plus_breakout
  strong_buy_score: 75
  buy_score: 60
  watch_score: 45
  b_tier_min_reward_risk: 1.6
  execution_reward_risk_cushion: 0.2
backtest:
  fee_rate: 0.001
  slippage_rate: 0.0005
  split_mode: calendar
  train_end_date: "2021-12-31"
  validation_end_date: "2023-12-31"
