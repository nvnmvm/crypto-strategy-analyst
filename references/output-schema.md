# 输出字段

分析 JSON 由 Pydantic 模型生成，并由 `schemas/analysis-report.schema.json` 约束。核心字段包括 UTC 时间、交易对、数据质量、三周期结论、区域、独立触碰/反弹/跌破计数、指标、确定性评分、入场/止损/止盈、投入金额、最大亏损、理由、失效条件、缺失数据与警告。回测固定切分字段名为 `time_splits`。

缺失的新闻、恐惧贪婪、链上、资金流和机器学习数据必须写为 `not_available`，不得用 `0` 或“中性”替代。
